export declare class AmharicTtsUtil {
    private static readonly letters;
    private static readonly ones;
    private static readonly tens;
    static translateNumberToAmharic(drawnNumber: string): string;
    static getAudioUrl(drawnNumber: string): string;
}
